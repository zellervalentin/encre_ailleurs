<%= simple_form_for(letter) do |f| %>
  <%= f.association :to, label:'Destinataire' %>
  <%= f.input :title, label:"Titre" %>
  <%= f.input :from, :as => :hidden, :value => current_user.id %>
  <%= f.input :message %>
  <%= f.button :submit, label:'Envoyer' %>
<% end %>
